# Goal: use the existing templated frontend, update the design / implementation, and make it usable for the current backend

feature description: review the frontend professionally, especially the statemanagement(authentication, session, user role, user info, global envirables, and the router guards), Basically focusing on the foundational layers. To make it easier later when meaningful components, views and pages to be built on a solid foundation

1. This design has to be a simple but production grade system design. This is the first version, it should be simple, clean and reliable.
2. Design the schemas as well, discuss the schema with me, as this is the hardest part.
3. Review the lastest API doc for Mastra AI, as it changing very fast.
4. I have an pre-designed folder structure for you to start with in `frontend`
5. this is a complex module, so discuss with me if you need.
6. remove any components / files / functions in the template frontend project(frontend)
  

## Actions to take
Design the execption module and output the design to [development/current_session/current_design]
